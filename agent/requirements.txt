# Optional: enables the real LangGraph StateGraph path in the agent.
# The agent runs the same graph through a deterministic fallback if this
# is not installed, so it is safe to skip for minimal demos.
langgraph>=0.2.60
