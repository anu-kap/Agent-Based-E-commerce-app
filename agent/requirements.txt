# Optional dependencies. The agent runs without either: it falls back
# to a deterministic graph if langgraph is missing, and silently skips
# all DB ops if psycopg is missing or DATABASE_URL is unset.
langgraph>=0.2.60
psycopg[binary]>=3.2.3
